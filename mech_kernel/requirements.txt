# MechKernel v2.6 verified runtime (Python 3.12, Windows x64)
# Keep the OCC/build123d pair pinned: newer combinations can change wrapped
# types and break STEP, boolean, and renderer compatibility.
build123d==0.11.1
cadquery-ocp-novtk==7.9.3.0

# 几何指标 + 渲染
trimesh>=4.0.0
numpy>=1.24.0
scipy>=1.10.0
Pillow>=10.0.0
requests>=2.31.0

# 可视化（如果需要 matplotlib 渲染）
matplotlib>=3.7.0

# v2.4 constraint solver / v2.6 validation and project formats
sympy>=1.12.0
scikit-learn>=1.3.0

# Test runner
pytest>=8.0.0
